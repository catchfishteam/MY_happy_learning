# 服务启动端口
server:
    port:  8080

# 数据库配置
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/trico?useSSL=false
        username: root
        password: shen0626
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.jdbc.Driver
        useSSL: false

# mybatis
mybatis:
    type-aliases-package: com.shenzk.web.dao
    mapper-locations: classpath:mapper/*.xml
    configLocation: classpath:mybatis-config.xml


# 日志输出
#logging:
#    file: D:/boot.log
#    level:
#        com.ibatis:DEBUG
#        root:DEBUG